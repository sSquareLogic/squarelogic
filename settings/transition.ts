export const duration = 0.4;
export const easing = "easeOut";
export const delay = 0;
export const type = "tween";

const defaultTransition = { duration, ease: easing, delay, type };

export default defaultTransition;
